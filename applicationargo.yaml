apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argoapp-argo-application
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/haima24/sd1087_msa
    targetRevision: HEAD
    path: Manifest-Azure
  destination: 
    server: https://kubernetes.default.svc
    namespace: argoapp

  syncPolicy:
    syncOptions:
    - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true